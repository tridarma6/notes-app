<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base Theme -->
    <style name="Base.Theme.NotesApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/red</item>
        <item name="android:statusBarColor">@color/green_active</item>

        <!-- NONAKTIFKAN GAYA BAWAAN UNTUK BUTTON -->
        <item name="buttonStyle">@style/PlainButtonStyle</item>
        <item name="materialButtonStyle">@style/PlainMaterialButtonStyle</item>
    </style>

    <!-- Final Theme -->
    <style name="Theme.NotesApp" parent="Base.Theme.NotesApp" />

    <style name="PlainButtonStyle" parent="Widget.AppCompat.Button">
        <item name="backgroundTint">@null</item>
    </style>

    <style name="PlainMaterialButtonStyle" parent="Widget.Material3.Button">
        <item name="backgroundTint">@null</item>
        <item name="rippleColor">@android:color/transparent</item>
    </style>
</resources>
